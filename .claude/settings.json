{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$TOOL_INPUT\" | grep -q 'git commit'; then cargo fmt 2>/dev/null; ruff format . 2>/dev/null; git add -u 2>/dev/null; fi"
          }
        ]
      }
    ]
  }
}
